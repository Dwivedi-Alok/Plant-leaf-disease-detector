# Plant-leaf-disease-detector
This model uses a convolutional neural network (CNN) for image classification. It processes images through convolutional layers for feature extraction, followed by max-pooling to reduce dimensionality. A global average pooling layer consolidates features, and dense layers output class predictions. Ideal for plant disease detection.
